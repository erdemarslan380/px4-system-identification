PX4 system identification and digital-twin methodology
======================================================

Purpose
-------
This text explains exactly how the repository identifies a multicopter model from PX4 logs, turns the result into a Gazebo SDF candidate, and validates that candidate as a digital twin. It is written as source material for a later paper, thesis chapter, or prompt-based writing workflow.

Repository scope
----------------
This repository contains the full system-identification workflow. The pipeline is intentionally split into two parts:
- `overlay/`: PX4 and Gazebo code that creates the maneuvers and records the logs.
- `experimental_validation/`: the offline estimators, SDF comparison scripts, scoring logic, and figure generators.

Core runtime components
-----------------------
1. `overlay/src/modules/custom_pos_control/custom_pos_control.cpp`
   - the minimal control-side entry point used in this repository.
   - for system identification, the important setting is `sysid` mode.
2. `overlay/src/modules/trajectory_reader/trajectory_reader.cpp`
   - publishes the reference setpoints for either trajectory playback or system-identification maneuvers.
   - writes PX4-side tracking logs and identification logs.
3. `overlay/src/modules/simulation/gz_plugins/system_identification_logger/SystemIdentificationLogger.cpp`
   - logs Gazebo truth values during SITL.
   - provides the simulator-side upper-bound reference.

Core offline components
-----------------------
1. `experimental_validation/cli.py`
   - estimates parameters from one maneuver log pair.
2. `experimental_validation/identification.py`
   - orchestrates full identification runs and prepares merged data tables.
3. `experimental_validation/estimators.py`
   - contains the actual mass, inertia, drag, and motor-model estimators.
4. `experimental_validation/compare_with_sdf.py`
   - compares identified parameters with the reference x500 SDF.
5. `experimental_validation/twin_metrics.py`
   - computes the blended digital-twin score and the family-level scores.
6. `experimental_validation/paper_artifacts.py`
   - creates trajectory overlays, robustness plots, error bars, and summary files.

Method overview
---------------
The identification method is built around a family of short, targeted maneuvers rather than a single monolithic experiment. Each maneuver is designed to isolate one parameter family as much as possible.

The nine built-in maneuvers are:
- `hover_thrust`
- `mass_vertical`
- `roll_sweep`
- `pitch_sweep`
- `yaw_sweep`
- `drag_x`
- `drag_y`
- `drag_z`
- `motor_step`

Each maneuver is triggered directly from the PX4 shell through `trajectory_reader set_ident_profile <profile>`.

The current overlay also provides a built-in campaign layer:
- `identification_only`
  - runs the nine maneuvers above in one uninterrupted session
- `full_stack`
  - runs the same nine maneuvers and then the five shipped validation trajectories

In both cases the module returns to the common anchor between items and does not log those repositioning legs.

Exact SITL execution flow
-------------------------
0. Clone PX4 into a dedicated identification tree:
   - `git clone https://github.com/PX4/PX4-Autopilot.git --recursive ~/PX4-Autopilot-Identification`
1. Sync the overlay into that dedicated PX4 tree:
   - `~/px4-system-identification/sync_into_px4_workspace.sh ~/PX4-Autopilot-Identification`
2. Build and start Gazebo SITL:
   - `cd ~/PX4-Autopilot-Identification`
   - `unset HEADLESS`
   - `make px4_sitl gz_x500`
   - if an older PX4 or Gazebo instance is still open, first:
     - `shutdown` in `pxh>` if that shell is alive,
     - `pkill -f '/PX4-Autopilot-Identification/build/px4_sitl_default/bin/px4' || true`
     - `pkill -f '/PX4-Autopilot-Identification/.*/gz/worlds/' || true`
     - `rm -f /tmp/px4_lock-0 /tmp/px4-sock-0`
3. In the PX4 shell:
   - `custom_pos_control start`
   - `trajectory_reader start`
   - `custom_pos_control enable`
   - `custom_pos_control set sysid`
   - `trajectory_reader set_mode identification`
   - `trajectory_reader set_ident_profile hover_thrust`
4. From QGroundControl:
   - arm
   - take off manually to about `3 m`
   - stabilize hover
   - switch to `OFFBOARD`
5. Run one maneuver at a time and wait for completion before sending the next one.
6. For a full truth-assisted x500 candidate, keep all nine maneuvers inside the same uninterrupted SITL session.

The now-preferred short execution path is the built-in campaign:
- in `pxh>`:
  - `custom_pos_control start`
  - `trajectory_reader start`
  - `custom_pos_control set px4_default`
  - `custom_pos_control enable`
  - `trajectory_reader ref 0 0 -3 0`
- from a second terminal:
  - `python3 ~/px4-system-identification/examples/run_mavlink_campaign.py --endpoint udpin:127.0.0.1:14550 --campaign full_stack --prepare-hover --heartbeat-warmup 5 --arm-attempts 10 --timeout 520`

That one command now covers:
- takeoff to the common hover point,
- the 9 identification maneuvers,
- the 5 validation trajectories,
- return-to-anchor transitions without extra CSV logs.

CubeOrange hardware build
-------------------------
The same overlay can be built for CubeOrange in the dedicated PX4 tree.

1. Sync the overlay with the CubeOrange board file:
   - `~/px4-system-identification/sync_into_px4_workspace.sh ~/PX4-Autopilot-Identification boards/cubepilot/cubeorange/default.px4board`
2. Build the hardware target:
   - `cd ~/PX4-Autopilot-Identification`
   - `make cubepilot_cubeorange_default`
3. The firmware artifact is:
   - `~/PX4-Autopilot-Identification/build/cubepilot_cubeorange_default/cubepilot_cubeorange_default.px4`
4. With the CubeOrange connected over USB, upload with:
   - `python3 ~/px4-system-identification/examples/upload_cubeorange_firmware.py`
   This helper targets `/dev/ttyACM0` and reduces the bootloader write chunk size to a value that completed reliably on the connected CubeOrange.
5. The same `custom_pos_control` and `trajectory_reader` shell commands described in this text are then used for hardware identification sorties and later validation flights.
6. If another board is used, replace the board file path and the build/upload target consistently for that board.

Before hardware identification or HITL trajectory playback, prepare the SD card from the workstation:
- `cd ~/px4-system-identification`
- `./examples/prepare_sdcard_payload.sh /media/$USER/<sdcard_mount_name>`

That step copies the five shipped trajectory binaries into:
- `/fs/microsd/trajectories/`

and creates the two hardware log roots:
- `/fs/microsd/tracking_logs/`
- `/fs/microsd/identification_logs/`

The current CubeOrange build footprint is now much lighter than before:
- flash use is about `74.9%`
- each CSV is flushed and closed when a run ends, so consecutive profiles or trajectories are intended to work in the same boot session without rebooting PX4

CubeOrange HIL/HITL check with jMAVSim
--------------------------------------
Use jMAVSim only in the HIL section of the workflow. The practical split on this machine is:
- current USB CDC device, for example `/dev/ttyACM0` or `/dev/ttyACM1`: jMAVSim
- QGroundControl: UDP only after jMAVSim is already running

Important:
- do not let QGroundControl and jMAVSim open the same serial device at the same time,
- the `newPosition > limit` jMAVSim exception is a corrupted serial-stream symptom, usually caused by two processes sharing one port.
- during firmware upload, close jMAVSim, QGroundControl, and any MAVLink shell before talking to the board bootloader.

Build jMAVSim once:
- `cd ~/PX4-Autopilot-Identification/Tools/simulation/jmavsim/jMAVSim`
- `ant create_run_jar copy_res`

Configure the HIL airframe once in QGroundControl:
- `SYS_AUTOSTART = 1001`
- `SYS_HITL = 1`

The current firmware/overlay combination was checked with:
- `pwm_out_sim` active in HIL mode,
- `HIL_ACT_FUNC1..4 = 101..104`,
- CubeOrange memory still comfortably below full use during HIL boot.

Before starting jMAVSim, list the live serial devices:
- `ls /dev/ttyACM* /dev/ttyUSB*`

Start jMAVSim on the current USB CDC device:
- `~/px4-system-identification/examples/start_jmavsim_hitl.sh ~/PX4-Autopilot-Identification <usb_cdc_device> 921600`

For a headless run:
- `PX4_SYSID_HEADLESS=1 ~/px4-system-identification/examples/start_jmavsim_hitl.sh ~/PX4-Autopilot-Identification <usb_cdc_device> 921600`

Use QGroundControl in UDP-only mode only after jMAVSim starts.

The practical HIL order is:
1. close the SITL QGroundControl window completely,
2. connect CubeOrange over USB,
3. use QGroundControl once to set `SYS_AUTOSTART = 1001` and `SYS_HITL = 1`,
4. reboot the board and close QGroundControl completely,
5. start jMAVSim on the current USB CDC device,
6. optionally reopen QGroundControl with only UDP auto-connect enabled,
7. run one of the campaign helpers over `udpin:127.0.0.1:14550`.

The practical SITL order is:
1. start Gazebo SITL,
2. optionally open QGroundControl only after Gazebo is already running,
3. leave QGroundControl passive while the helper drives the campaign,
4. close QGroundControl fully before moving on to HIL.

RC-assisted operator workflow
-----------------------------
For HIL or real-flight operation, the RC path can select controller, workflow, and item independently:
- `CST_RC_SEL_EN = 1`
- `CST_RC_CTRL_CH = <controller_pot>`
- `TRJ_RC_MODE_EN = 1`
- `TRJ_RC_MODE_CH = <workflow_pot>`
- `TRJ_RC_SEL_EN = 1`
- `TRJ_RC_SEL_CH = <item_pot>`
- `TRJ_RC_MIN_ID = 100`
- `TRJ_RC_MAX_ID = 104`
- `TRJ_RC_START_EN = 1`
- `TRJ_RC_START_BTN = <H_button_index>`

Workflow slots:
- `0`: hold position
- `1`: one identification maneuver
- `2`: one trajectory
- `3`: `identification_only`
- `4`: `trajectory_only`
- `5`: `full_stack`

The `H` button applies the currently selected workflow. In single-identification mode the item pot selects one of the `9` identification profiles. In single-trajectory mode the item pot selects one trajectory id in `100..104` by default.

QGroundControl calibration notes:
- use `Vehicle Setup > Radio` first and complete the normal radio calibration,
- while calibrating, note which spare controls show up as `AUX 1..6`,
- `CST_RC_CTRL_CH`, `TRJ_RC_MODE_CH`, and `TRJ_RC_SEL_CH` use those `AUX 1..6` numbers and therefore map to `manual_control_setpoint.aux1..aux6`,
- if the `H` trigger is coming from a QGroundControl joystick/manual-control button, assign it in `Vehicle Setup > Joystick` and use the resulting one-based button index for `TRJ_RC_START_BTN`,
- verify the live mapping with `listener manual_control_setpoint` before flight.

The required live checks are:
- the chosen pots move the intended `auxN` fields,
- the `H` trigger toggles `manual_control_setpoint.buttons`.

If the button path does not toggle `buttons`, do not rely on the RC trigger and use the helper script or shell commands for the actual start command.

Keep the PX4 flight-mode switch separate from the repository-specific workflow selection:
- `Vehicle Setup > Flight Modes` handles `Position`, `Offboard`, and the normal PX4 nav states,
- the repository RC parameters above choose one identification profile, one trajectory, or one built-in campaign.

During HIL, a physical RC receiver may stay connected while `jMAVSim` is running. The simulator uses the USB CDC MAVLink link and does not replace the board's RC input wiring. This is the correct electrical smoke test for radio calibration, arming, and `Position <-> Offboard` transitions before real flight.

For an RC-driven HIL mode-switch smoke test:
- connect the receiver before boot,
- complete `Vehicle Setup > Radio`,
- in `Vehicle Setup > Flight Modes`, assign one switch position to `Position` and one to `Offboard`,
- set `COM_RC_IN_MODE = 0`,
- start `custom_pos_control`, `trajectory_reader`, and a hold reference,
- arm in `Position`, climb to hover, then flip the transmitter switch `Position -> Offboard -> Position`.

When using the scripted HIL helper, the default automation setting is still to disable manual-control inputs during the scripted hover preparation. If the goal is to keep the physical receiver active during the scripted HIL run, pass `--manual-control-mode 0`.

Supported HIL command shapes now mirror SITL:
- full campaign:
  - `python3 ~/px4-system-identification/examples/run_mavlink_campaign.py --endpoint udpin:127.0.0.1:14550 --campaign full_stack --prepare-hover --manual-control-mode 4 --allow-missing-local-position --blind-hover-seconds 12 --timeout 520`
- identification only:
  - `python3 ~/px4-system-identification/examples/run_mavlink_campaign.py --endpoint udpin:127.0.0.1:14550 --campaign identification_only --prepare-hover --manual-control-mode 4 --allow-missing-local-position --blind-hover-seconds 12 --timeout 420`
- trajectories only:
  - `python3 ~/px4-system-identification/examples/run_mavlink_campaign.py --endpoint udpin:127.0.0.1:14550 --campaign trajectory_only --prepare-hover --manual-control-mode 4 --allow-missing-local-position --blind-hover-seconds 12 --timeout 220`
- one identification maneuver:
  - `python3 ~/px4-system-identification/examples/run_hitl_udp_sequence.py --endpoint udpin:127.0.0.1:14550 --kind ident --name hover_thrust --allow-missing-local-position --blind-hover-seconds 12`
- one trajectory:
  - `python3 ~/px4-system-identification/examples/run_hitl_udp_sequence.py --endpoint udpin:127.0.0.1:14550 --kind trajectory --traj-id 100 --allow-missing-local-position --blind-hover-seconds 12`

PX4 console behavior during a maneuver
--------------------------------------
The current implementation prints the operational state explicitly:
- `Identification profile set to ...`
- `Purpose: ...`
- `Estimated duration: ...`
- `Identification maneuver started: ...`
- `Identification log started: ...`
- `Identification maneuver completed: ...`
- `Identification log completed: ...`
- `Tracking log completed: ...`

This means the operator does not need to guess when a maneuver ends. The end-of-maneuver condition is now visible from the PX4 console itself.

The current implementation also closes and flushes each CSV at the end of a run. The operator can therefore execute multiple identification profiles or multiple validation trajectories in one PX4 boot session without rebooting between them.

Interactive review of hardware logs
-----------------------------------
After a HITL or real-flight session, the intended review flow is:
1. remove the SD card and mount it on the workstation,
2. import the logs into the repository:
   - `./examples/import_sdcard_logs.sh /media/$USER/<sdcard_mount_name> ~/px4-system-identification/hitl_runs/session_001`
3. build the review bundle:
   - `python3 experimental_validation/build_hitl_review_bundle.py --log-root ~/px4-system-identification/hitl_runs/session_001 --out-dir ~/px4-system-identification/hitl_runs/session_001/review`
4. open:
   - `~/px4-system-identification/hitl_runs/session_001/review/index.html`

The resulting page provides a zoomable and pannable 3D path plot, X/Y/Z versus time plots, basic summary metrics, and copied raw CSV files. The imported logs and generated review bundle can both be committed into the repository for later inspection or GitHub sharing.

If the board remains connected over USB CDC after HITL or a real flight, the SD card does not need to be removed. Use:
- `ls /dev/ttyACM*`
- `python3 ~/px4-system-identification/examples/pull_sdcard_logs_over_mavftp.py --port <usb_cdc_device> --baud 57600 --destination-dir ~/px4-system-identification/hitl_runs/session_001`

If you want to inspect the card first, use the MAVFTP browser:
- `python3 ~/px4-system-identification/examples/sdcard_browser.py --serial-port <usb_cdc_device> --baud 57600`
- open `http://127.0.0.1:8765/`

The browser lets the operator open `/fs/microsd`, inspect `tracking_logs`, `identification_logs`, and `log`, then pull selected files into:
- `~/px4-system-identification/hitl_runs/browser_downloads/`

Before either USB CDC method:
- close `jMAVSim`,
- close `QGroundControl`,
- close any MAVLink shell on the same USB CDC device.

Workspace convention
--------------------
The repository assumes one dedicated PX4 clone at:
- `~/PX4-Autopilot-Identification`

That tree belongs only to this identification workflow.

Helper scripts for the operator
-------------------------------
Two small helper scripts are now part of the repository:
- `~/px4-system-identification/examples/run_repo_smoke_test.sh`
  - runs the shipped repository tests and confirms the documentation state, validation-trajectory generator, and figure-generation pipeline.
- `~/px4-system-identification/examples/refresh_demo_assets.sh ~/PX4-Autopilot-Identification`
  - installs the five shipped validation trajectories into the PX4 workspace,
  - rebuilds the shipped Stage-1 proxy inputs,
  - regenerates the current figure set,
  - updates `examples/paper_assets/paper_validation_summary.json`.
- `~/px4-system-identification/experimental_validation/build_latest_x500_candidate.py`
  - selects the latest available identification CSV for each required maneuver family,
  - checks whether any required profile is missing,
  - automatically uses the latest suitable truth CSV from `rootfs/sysid_truth_logs/`,
  - writes the comparison package into the requested output folder.
- `~/px4-system-identification/examples/import_sdcard_logs.sh`
  - copies `tracking_logs/*.csv` and `identification_logs/*.csv` from a mounted SD card into a versionable folder inside this repository.
- `~/px4-system-identification/examples/pull_sdcard_logs_over_mavftp.py`
  - pulls `tracking_logs/*.csv` and `identification_logs/*.csv` over the live USB CDC / MAVFTP connection without removing the SD card,
  - enforces single-process access to `/dev/ttyACM0`,
  - skips locally complete files and writes a `pull_report.json` summary.
- `~/px4-system-identification/experimental_validation/build_hitl_review_bundle.py`
  - builds a static HTML review bundle with a zoomable and pannable 3D path plot, X/Y/Z versus time plots, summary metrics, and raw CSV copies.

Maneuver definitions
--------------------
1. `hover_thrust`
   - purpose: excite the hover-thrust estimator around a steady hover point.
   - duration: about `26 s`.
   - output family: hover thrust tracking and part of thrust scaling.
   - after completion: the vehicle holds the last reference.

2. `mass_vertical`
   - purpose: excite the vertical dynamics with a multi-frequency vertical command.
   - duration: about `36 s`.
   - output family: total mass and thrust scale.
   - after completion: the vehicle holds the last reference.

3. `roll_sweep`
   - purpose: excite roll-axis inertia and cross-axis coupling through lateral motion.
   - duration: about `28 s`.
   - output family: primarily `Ixx` and roll coupling terms.
   - after completion: the vehicle holds the last reference.

4. `pitch_sweep`
   - purpose: excite pitch-axis inertia and coupling.
   - duration: about `28 s`.
   - output family: primarily `Iyy` and pitch coupling terms.
   - after completion: the vehicle holds the last reference.

5. `yaw_sweep`
   - purpose: excite yaw inertia and yaw moment balance.
   - duration: about `24 s`.
   - output family: primarily `Izz` and yaw-related moment terms.
   - after completion: the vehicle holds the last reference.

6. `drag_x`
   - purpose: excite the X-axis translational drag.
   - duration: about `30 s`.
   - output family: forward drag-related terms.
   - after completion: the vehicle holds the last reference.

7. `drag_y`
   - purpose: excite the Y-axis translational drag.
   - duration: about `30 s`.
   - output family: lateral drag-related terms.
   - after completion: the vehicle holds the last reference.

8. `drag_z`
   - purpose: excite the Z-axis drag and vertical damping.
   - duration: about `30 s`.
   - output family: vertical drag-related terms.
   - after completion: the vehicle holds the last reference.

9. `motor_step`
   - purpose: excite actuator dynamics through a step-like thrust profile.
   - duration: about `24 s`.
   - output family: motor time constants and motor dynamic terms.
   - after completion: the vehicle holds the last reference.

Recorded quantities
-------------------
The PX4 identification CSV includes, at minimum:
- reference position, velocity, acceleration, yaw, and yaw rate,
- vehicle position, velocity, acceleration,
- attitude and body rates,
- attitude setpoint and rate setpoint,
- actuator outputs,
- ESC rpm, voltage, and current if available,
- hover thrust estimate,
- controller label.

The Gazebo truth CSV includes simulator-side quantities such as:
- body accelerations,
- rotor speeds,
- thrust-related truth terms,
- vehicle truth state.

Exact SITL log directories
--------------------------
- PX4 identification logs:
  - `~/PX4-Autopilot-Identification/build/px4_sitl_default/rootfs/identification_logs/`
- PX4 tracking logs:
  - `~/PX4-Autopilot-Identification/build/px4_sitl_default/rootfs/tracking_logs/`
- Gazebo truth logs:
  - `~/PX4-Autopilot-Identification/build/px4_sitl_default/rootfs/sysid_truth_logs/`

The current repository version activates the truth logger in normal manual SITL as well. That means the operator no longer needs a separate special launch path to obtain truth-assisted upper-bound logs.
For a clean new full-family run, the operator may delete the old CSV files in those three directories, but only while SITL is stopped.

On hardware, the corresponding PX4 storage directories are typically:
- `/fs/microsd/identification_logs/`
- `/fs/microsd/tracking_logs/`

Offline estimation flow
-----------------------
The offline estimation is performed in three conceptual layers.

Layer 1: one-log estimation
- `experimental_validation/cli.py` reads one PX4 identification CSV and, optionally, one Gazebo truth CSV.
- It writes:
  - `identified_parameters.json`
  - `candidate_inertial.sdf.xml`
  - `candidate_vehicle_params.yaml`

Layer 2: full maneuver-family fusion
- `experimental_validation/build_latest_x500_candidate.py` is now the operator-facing wrapper around the full-family fusion step.
- Internally it uses `experimental_validation/compare_with_sdf.py` to combine:
  - `hover_thrust`
  - `mass_vertical`
  - `roll_sweep`
  - `pitch_sweep`
  - `yaw_sweep`
  - `drag_x`
  - `drag_y`
  - `drag_z`
  - `motor_step`
- It then compares the candidate with the x500 SDF and writes:
  - `identified_parameters.json`
  - `identified_parameters_by_mode.json`
  - `sdf_reference.json`
  - `sdf_comparison.json`
  - `sdf_comparison_by_mode.json`
  - `used_identification_logs.json`
  - `candidate_x500_base.sdf`

The full-family helper is intentionally stricter than the old manual `ls -1t ...` workflow:
- it refuses to proceed silently when a required profile is missing,
- it records which exact log files were chosen,
- it records which truth file was used,
- and it reports the effective primary mode (`px4_only`, `telemetry_augmented`, or `truth_assisted`).

This helper intentionally replaces the older manual shell pattern in which the operator assigned variables such as `HOVER=$(ls -1t ...)`, `ROLL=$(ls -1t ...)`, and then called `compare_with_sdf.py` directly. That older pattern was too brittle when a profile had been rerun several times or when a truth log was missing from the latest manual session.

Layer 3: figure generation and statistical validation
- `experimental_validation/paper_artifacts.py` generates:
  - five trajectory overlay plots,
  - stress-test surface plots,
  - stress-test line plots,
  - parameter error bars,
  - family score bars,
  - trajectory summary score plots,
  - `paper_validation_summary.json`.
- `experimental_validation/generate_placeholder_sitl_runs.py` creates a reproducible two-SITL Stage-1 input bundle:
  - `stock x500 SITL proxy`
  - `identified digital twin SITL`
- `experimental_validation/sitl_validation_artifacts.py` rebuilds the final paper figures from those run roots.

Current shipped figure package
------------------------------
The repository already ships one complete figure package under:
- `~/px4-system-identification/examples/paper_assets/`

The main outputs are:
- `paper_validation_summary.json`
- `figures/trajectory_rmse_summary.png`
- `figures/parameter_error_bars.png`
- the five trajectory overlay figures
- the five stress-test surface figures
- the five stress-test line figures

The currently shipped Stage-1 blue-side data are `stock x500 SITL proxy` logs, not real-flight logs. This is intentional. It makes the full appendix reproducible now, and later the stock-proxy CSV files can be replaced with real-flight CSV files without changing the figure-generation path.

Validation modes
----------------
Three validation modes are supported:
- `px4_only`
- `telemetry_augmented`
- `truth_assisted`

Interpretation:
- `px4_only` is the most realistic mode for real-flight transfer.
- `telemetry_augmented` adds measurements that are still feasible to log outside the simulator.
- `truth_assisted` is the simulator-side upper bound. If this mode cannot recover the hidden x500 parameters, the method is not ready.

Current strongest claim
-----------------------
At the current checkpoint, the strongest validated claim is:
- truth-assisted SITL can recover the comparable x500 SDF terms to numerical precision,
- the figure-generation pipeline is complete,
- the repository can now show the complete operator flow from maneuver execution to SDF comparison and paper figures.

This is the correct upper-bound milestone. It does not automatically imply that real-flight digital-twin equivalence has already been proven.

Practical troubleshooting note
------------------------------
During manual SITL it is possible to see messages such as:
- `NodeShared::Publish() Error: Interrupted system call`
- `vehicle_imu ... gyro timestamp error`
- `vehicle_imu ... accel timestamp error`

In the current workflow those messages usually indicate that Gazebo transport or the SITL session was interrupted during shutdown or rapid restart, especially near the end of a maneuver. The correct operator response is:
1. stop PX4 cleanly with `shutdown`,
2. restart `make px4_sitl gz_x500`,
3. rerun only the affected maneuver,
4. rebuild the candidate with `build_latest_x500_candidate.py`.

The repository also now patches the manual x500 model so Gazebo truth logs are written automatically into:
- `~/PX4-Autopilot-Identification/build/px4_sitl_default/rootfs/sysid_truth_logs/`

If that directory is missing, the operator should resync the overlay and rebuild before interpreting the comparison results.
If the directory exists but remains empty after a completed maneuver, the operator should still treat the run as invalid for truth-assisted comparison. In that case the most likely cause is that the Gazebo launcher was started without the required system-plugin search path, and the correct action is to resync the overlay, restart SITL, and rerun the maneuver.

Another practical operator rule is that the active truth CSV should not be deleted while SITL is still running. In earlier versions that caused Gazebo to continue writing into an already deleted file descriptor, which made `sysid_truth_logs/` look empty even though the logger had not actually stopped. The current repository version now reopens the file automatically if it is removed during an active run, but the preferred workflow is still to delete old truth logs only before startup or after shutdown.

Five validation trajectories
----------------------------
The repository uses five validation trajectories:
- `hairpin`
- `lemniscate`
- `circle`
- `time_optimal_30s`
- `minimum_snap_50s`

The shipped binary files are:
- `id_100.traj`: `hairpin`, nominally `23 s`
- `id_101.traj`: `lemniscate`, nominally `19 s`
- `id_102.traj`: `circle`, nominally `15 s`
- `id_103.traj`: `time_optimal_30s`, nominally `11 s`
- `id_104.traj`: `minimum_snap_50s`, nominally `14 s`

These files are used as-is across SITL, HITL, and figure generation. The repository does not synthesize replacement validation trajectories anymore.

Operational rule for both SITL and real flight:
1. take off and stabilize at about `3 m`,
2. keep the common anchor at `(0, 0, -3)` in local NED,
3. begin logging only after the vehicle reaches a stable hover near that anchor,
4. execute the trajectory,
5. return to hover if another trajectory will follow.

This keeps the overlays meaningful because all runs still use the same anchor and reference frame, even though the shipped trajectories do not all return to the start point.

Two-stage paper result flow
---------------------------
Stage 1: real-flight versus digital-twin overlays
- identify the model from the four identification sorties,
- write the estimated parameters into a Gazebo SDF candidate,
- run the five validation trajectories in simulation,
- collect the corresponding real-flight trajectory logs,
- overlay real flight and digital twin on the same axes,
- report RMSE, delay, and blended twin score.

Current repository state before real-flight logs exist:
- the Stage-1 structure is already populated with two SITL roots:
  - `~/px4-system-identification/examples/paper_assets/stage1_inputs/stock_sitl_proxy/`
  - `~/px4-system-identification/examples/paper_assets/stage1_inputs/digital_twin_sitl/`
- the blue-side root is intentionally a stock x500 SITL proxy with mild smooth perturbation,
- the orange-side root is the identified digital twin SITL run,
- later, only the blue-side CSV files need to be replaced with real-flight logs.

Stage 2: SITL statistical stress tests
- keep the identified twin fixed,
- perturb the reference plant across multiple families,
- generate surfaces and slices for:
  - payload mass versus payload Z offset,
  - payload mass versus payload X offset,
  - payload mass versus payload Y offset,
  - payload mass versus arm-length scale,
  - payload mass versus motor-dynamics scale,
- use those plots to show robustness and failure sensitivity.

Important interpretation of Stage 2
-----------------------------------
These surfaces are not meant to claim real-flight equivalence by themselves. They are robustness plots. Their role is to show how the fixed identified twin behaves when the plant is perturbed.

Surface-height interpretation:
- the vertical axis is the twin-similarity score in percent,
- `100%` means the perturbed plant still looks almost indistinguishable from the fixed twin under that stress condition,
- lower values indicate where the current identified model is more fragile.

The line plots are not separate experiments. They are readable slices through the same surfaces.

Real-flight sortie plan
-----------------------
Recommended calm-day sequence:
- Sortie 1: `hover_thrust`, `mass_vertical`
- Sortie 2: `roll_sweep`, `pitch_sweep`
- Sortie 3: `yaw_sweep`, `motor_step`
- Sortie 4: `drag_x`, `drag_y`, `drag_z`
- Sortie 5 and later: the five validation trajectories

Every sortie uses the same operational rule:
- start helper modules,
- take off manually,
- stabilize at about `3 m`,
- enter OFFBOARD,
- run one profile,
- wait for the completion messages,
- recover to hover,
- continue only if the safety and battery margin remain good.

SDF update workflow
-------------------
After estimation, the inertial snippet written by the tool is stored in `candidate_inertial.sdf.xml`. The recommended practical workflow is:
1. duplicate the stock x500 Gazebo model into a new model folder such as `~/PX4-Autopilot-Identification/Tools/simulation/gz/models/x500_identified/`,
2. replace the relevant inertial and motor parameters with the identified values,
3. leave the stock x500 model untouched for comparison,
4. rerun the five validation trajectories with the identified model,
5. generate the overlay figures again.

This repository also provides the direct helper:
- `python3 experimental_validation/prepare_identified_model.py --px4-root ~/PX4-Autopilot-Identification --candidate-dir ~/px4-system-identification/experimental_validation/outputs/x500_candidate --model-name x500_identified`

Real-flight calibration restore after firmware update
-----------------------------------------------------
This repository also supports restoring calibration-related parameters after a firmware update.

Place the QGroundControl export at:
- `~/px4-system-identification/experimental_validation/qgc/current_vehicle.params`

Then run:
- `python3 experimental_validation/calibration_restore.py --input ~/px4-system-identification/experimental_validation/qgc/current_vehicle.params --out-dir ~/px4-system-identification/experimental_validation/qgc/restore`

Files to cite in a paper appendix
---------------------------------
Runtime files:
- `overlay/src/modules/custom_pos_control/custom_pos_control.cpp`
- `overlay/src/modules/trajectory_reader/trajectory_reader.cpp`
- `overlay/src/modules/simulation/gz_plugins/system_identification_logger/SystemIdentificationLogger.cpp`

Offline files:
- `experimental_validation/identification.py`
- `experimental_validation/estimators.py`
- `experimental_validation/compare_with_sdf.py`
- `experimental_validation/twin_metrics.py`
- `experimental_validation/paper_artifacts.py`

Key generated artifacts:
- `~/px4-system-identification/examples/paper_assets/paper_validation_summary.json`
- `~/px4-system-identification/examples/paper_assets/figures/`
- `~/px4-system-identification/experimental_validation/outputs/`

Current shipped figures and values
----------------------------------
Generated with:
- `python3 experimental_validation/generate_sitl_validation_bundle.py --mode placeholder --candidate-dir ~/px4-system-identification/examples/paper_assets/candidates/x500_truth_assisted_sitl_v1 --run-out-root ~/px4-system-identification/examples/paper_assets/stage1_inputs --paper-assets-root ~/px4-system-identification/examples/paper_assets`

Current upper-bound model score:
- blended twin score: `100.00 / 100`
- family scores:
  - inertia: `100.00`
  - mass: `100.00`
  - motor coefficients: `100.00`
  - motor dynamics: `100.00`

Current Stage-1 trajectory RMSE values, blue-side stock SITL proxy vs orange-side digital twin SITL:
- `hairpin`: `0.451 m` vs `0.150 m`
- `lemniscate`: `0.336 m` vs `0.113 m`
- `circle`: `0.438 m` vs `0.111 m`
- `time_optimal_30s`: `0.644 m` vs `0.450 m`
- `minimum_snap_50s`: `0.597 m` vs `0.341 m`

Current Stage-2 mean similarity values:
- payload mass vs Z offset: `64.09%`
- payload mass vs X offset: `67.21%`
- payload mass vs Y offset: `66.40%`
- payload mass vs arm-length scale: `54.16%`
- motor constant vs motor time-scale: `72.15%`

How to swap in real-flight logs later
-------------------------------------
1. Keep the orange-side digital twin root:
   - `~/px4-system-identification/examples/paper_assets/stage1_inputs/digital_twin_sitl/`
2. Replace the blue-side CSV files under:
   - `~/px4-system-identification/examples/paper_assets/stage1_inputs/stock_sitl_proxy/tracking_logs/`
3. Keep or recreate the filenames:
   - `hairpin.csv`
   - `lemniscate.csv`
   - `circle.csv`
   - `time_optimal_30s.csv`
   - `minimum_snap_50s.csv`
4. Rerun:
   - `python3 experimental_validation/sitl_validation_artifacts.py --out-dir ~/px4-system-identification/examples/paper_assets --stock-root ~/px4-system-identification/examples/paper_assets/stage1_inputs/stock_sitl_proxy --twin-root ~/px4-system-identification/examples/paper_assets/stage1_inputs/digital_twin_sitl --candidate-json ~/px4-system-identification/examples/paper_assets/candidates/x500_truth_assisted_sitl_v1/identified_parameters.json`
